#pragma once
//herencia simple se deja asi
// polimorfismo estatico se meten varias metodos de mismo nombre y tipo de retorno (misma clase)
// polimorfismo dinamico se meten varias metodos de mismo nombre y tipo de retorno en subclases y superclase
// clase abstracta es que tiene metodo virtual puro, virtual igualado a 0 "virtual int nombre() = 0;"
#include "SuperClass.h"

class SubClass : public SuperClass {
private:
public:

};